import { StyleSheet } from "react-native";

export const SecondaryBtnStyle = StyleSheet.create({
    button: {
        padding: 10,
        borderRadius: 50,
        width: '100%',
        borderColor: '#1E90FF',
        borderWidth: 2,
    },
    buttonText: {
        color: '#1E90FF',
        fontSize: 16,
        fontWeight: "500"
    }
});
